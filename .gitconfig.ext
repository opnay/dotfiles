[alias]
	c = commit
	cp = cherry-pick
	ca = commit --amend
	one = log --graph --all --pretty=format:'%h -%C(yellow)%d%C(reset) %s%C(reset)'
	f = fetch
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
